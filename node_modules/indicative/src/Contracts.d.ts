import { ErrorFormatterContract } from 'indicative-compiler';
import { Schema, Messages } from 'indicative-parser';
export declare type ValidatorConfig = {
    cacheKey?: string;
    existyStrict: boolean;
    removeAdditional: boolean;
    formatter: {
        new (): ErrorFormatterContract;
    };
};
export declare type SanitizerConfig = {
    cacheKey?: string;
};
export declare type ValidateFn = (data: any, schema: Schema, messages?: Messages, config?: Partial<ValidatorConfig>) => Promise<any>;
export declare type SanitizeFn = (data: any, schema: Schema, config?: Partial<SanitizerConfig>) => any;
