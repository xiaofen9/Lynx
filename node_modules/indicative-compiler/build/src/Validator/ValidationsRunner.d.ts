import { ParsedRule, ParsedRulesMessages } from 'indicative-parser';
import { ValidationDataRoot, ValidationDefination, CollectorContract } from '../contracts';
export declare class ValidationsRunner {
    private _field;
    private _type;
    private _dotPath;
    private _fieldMessages;
    private _genericMessages;
    async: boolean;
    private _validations;
    private _pointer;
    constructor(_field: string, _type: 'literal' | 'object' | 'array', _dotPath: string[], rules: ParsedRule[], validations: {
        [key: string]: ValidationDefination;
    }, _fieldMessages: ParsedRulesMessages, _genericMessages: ParsedRulesMessages);
    private _computeValidations;
    private _getDataCopy;
    private _reportValueToCollector;
    private _reportErrorToCollector;
    exec(data: ValidationDataRoot, collector: CollectorContract, config: unknown, bail?: boolean): boolean;
    execAsync(data: ValidationDataRoot, collector: CollectorContract, config: unknown, bail?: boolean): Promise<boolean>;
}
