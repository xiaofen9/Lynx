import { Schema, Messages } from 'indicative-parser';
import { ArrayWrapper } from './ArrayWrapper';
import { ValidationsRunner } from './ValidationsRunner';
import { ValidationDefination } from '../contracts';
export declare class Compiler {
    private _validations;
    private _parsedSchema;
    private _parsedMessages;
    constructor(schema: Schema, messages: Messages, _validations: {
        [key: string]: ValidationDefination;
    });
    compile(): (ValidationsRunner | ArrayWrapper)[];
}
