"main";_22e‍.x([["themeSettings",()=>themeSettings],["text",()=>text],["language",()=>language],["api",()=>api],["initOnClient",()=>initOnClient],["initOnServer",()=>initOnServer]]);let themeSettings = null;
       let text = null;
       let language = null;
       let api = null;

const setVariables = options => {
	if (options.themeSettings) {
		(_22e‍.u({ themeSettings } = options));
	}

	if (options.text) {
		(_22e‍.u({ text } = options));
	}

	if (options.language) {
		(_22e‍.u({ language } = options));
	}

	if (options.api) {
		(_22e‍.u({ api } = options));
	}
};

       const initOnClient = options => {
	setVariables(options);
};

       const initOnServer = options => {
	setVariables(options);
};