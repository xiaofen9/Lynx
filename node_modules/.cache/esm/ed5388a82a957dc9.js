"main";_2a9‍.x([["default",()=>CartIndicator]]);let React;_2a9‍.w("react",[["default",["React"],function(v){React=v}]]);let NavLink;_2a9‍.w("react-router-dom",[["NavLink",["NavLink"],function(v){NavLink=v}]]);let themeSettings,text;_2a9‍.w("../../lib/settings",[["themeSettings",["themeSettings"],function(v){themeSettings=v}],["text",["text"],function(v){text=v}]]);



const CartCount = ({ cart }) => {
	if (cart && cart.items && cart.items.length > 0) {
		const itemsCount = cart.items.reduce((a, b) => a + b.quantity, 0);
		return React.createElement(
			'span',
			{ className: 'cart-count' },
			itemsCount
		);
	}
	return null;
};

const CartIcon = ({ cartIsActive }) => {
	if (cartIsActive) {
		return React.createElement('img', {
			src: '/assets/images/close.svg',
			className: 'icon',
			alt: text.close,
			title: text.close,
			style: { minWidth: 24, padding: 4 }
		});
	}
	return React.createElement('img', {
		src: '/assets/images/shopping-bag.svg',
		className: 'icon',
		alt: text.cart,
		title: text.cart,
		style: { minWidth: 24 }
	});
};

               class CartIndicator extends React.PureComponent {
	render() {
		const { cart, onClick, cartIsActive } = this.props;
		return React.createElement(
			'span',
			{ className: 'cart-button', onClick: onClick },
			React.createElement(CartIcon, { cartIsActive: cartIsActive }),
			React.createElement(CartCount, { cart: cart })
		);
	}
}