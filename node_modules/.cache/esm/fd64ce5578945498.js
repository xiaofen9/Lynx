"main";let React;_789‍.w("react",[["default",["React"],function(v){React=v}]]);let injectStripe;_789‍.w("react-stripe-elements",[["injectStripe",["injectStripe"],function(v){injectStripe=v}]]);let CardSection;_789‍.w("./CardSection",[["default",["CardSection"],function(v){CardSection=v}]]);



class CheckoutForm extends React.Component {
	constructor(props) {
		super(props);
		this.state = {
			inProgress: false
		};
		this.submit = this.submit.bind(this);
	}

	async submit(ev) {
		this.setState({
			inProgress: true
		});
		const { formSettings, onCreateToken, stripe } = this.props;
		const { token } = await stripe.createToken({
			name: formSettings.email
		});
		if (token && token !== 'undefined') {
			onCreateToken(token.id);
		} else {
			this.setState({
				inProgress: false
			});
		}
	}

	render() {
		const { inProgress } = this.state;
		return React.createElement(
			'div',
			null,
			React.createElement(CardSection, { title: 'Credit Card details' }),
			React.createElement(
				'div',
				{ className: 'checkout-button-wrap' },
				React.createElement(
					'button',
					{
						onClick: this.submit,
						disabled: inProgress,
						className: `checkout-button button is-primary${inProgress ? ' is-loading' : ''}`
					},
					'Confirm order'
				)
			)
		);
	}
}
_789‍.d(injectStripe(CheckoutForm));