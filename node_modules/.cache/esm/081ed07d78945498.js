"main";_789‍.x([["themeSettings",()=>themeSettings],["text",()=>text],["language",()=>language],["api",()=>api],["initOnClient",()=>initOnClient],["initOnServer",()=>initOnServer]]);let themeSettings = null;
       let text = null;
       let language = null;
       let api = null;

const setVariables = options => {
	if (options.themeSettings) {
		(_789‍.u({ themeSettings } = options));
	}

	if (options.text) {
		(_789‍.u({ text } = options));
	}

	if (options.language) {
		(_789‍.u({ language } = options));
	}

	if (options.api) {
		(_789‍.u({ api } = options));
	}
};

       const initOnClient = options => {
	setVariables(options);
};

       const initOnServer = options => {
	setVariables(options);
};