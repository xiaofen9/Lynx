"main";_2a9‍.x([["default",()=>CustomPageList]]);let React;_2a9‍.w("react",[["default",["React"],function(v){React=v}]]);let api;_2a9‍.w("../../lib/api",[["default",["api"],function(v){api=v}]]);let PageList;_2a9‍.w("./list",[["default",["PageList"],function(v){PageList=v}]]);



               class CustomPageList extends React.Component {
	constructor(props) {
		super(props);

		this.fetchData = ({ tags, sort }) => {
			const filter = {
				tags: tags,
				sort: sort
			};

			api.ajax.pages.list(filter).then(({ status, json }) => {
				this.setState({
					pages: json
				});
			});
		};

		this.state = {
			pages: []
		};
	}

	componentDidMount() {
		this.fetchData(this.props);
	}

	componentWillReceiveProps(nextProps) {
		this.fetchData(nextProps);
	}

	render() {
		const { pages } = this.state;
		return React.createElement(PageList, { pages: pages });
	}
}