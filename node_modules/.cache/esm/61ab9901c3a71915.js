"main";let db;_c3a‍.w("../../lib/mongo",[["db",["db"],function(v){db=v}]]);

class PaymentMethodsLightService {
	constructor() {}

	getMethods(filter = {}) {
		return db
			.collection('paymentMethods')
			.find(filter)
			.toArray()
			.then(items => items.map(item => this.changeProperties(item)));
	}

	changeProperties(item) {
		if (item) {
			item.id = item._id.toString();
			delete item._id;
		}
		return item;
	}
}

_c3a‍.d(new PaymentMethodsLightService());
