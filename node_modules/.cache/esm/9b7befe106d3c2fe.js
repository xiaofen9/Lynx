"main";_06d‍.x([["themeSettings",()=>themeSettings],["text",()=>text],["language",()=>language],["api",()=>api],["initOnClient",()=>initOnClient],["initOnServer",()=>initOnServer]]);let themeSettings = null;
       let text = null;
       let language = null;
       let api = null;

const setVariables = options => {
	if (options.themeSettings) {
		(_06d‍.u({ themeSettings } = options));
	}

	if (options.text) {
		(_06d‍.u({ text } = options));
	}

	if (options.language) {
		(_06d‍.u({ language } = options));
	}

	if (options.api) {
		(_06d‍.u({ api } = options));
	}
};

       const initOnClient = options => {
	setVariables(options);
};

       const initOnServer = options => {
	setVariables(options);
};